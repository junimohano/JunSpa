import { OctagonGirlFilterPipe } from './octagon-girl-filter.pipe';

describe('OctagonGirlFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OctagonGirlFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
