import { FighterFilterPipe } from './fighter-filter.pipe';

describe('FighterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FighterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
